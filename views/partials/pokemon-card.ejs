<div class="pokemon-card">
    <h3><%= pokemon.name %></h3>
    <img src="/public/images/poke-ball.jpeg" alt="<%= pokemon.name %>">
    <p><strong>Type:</strong> <%= pokemon.type.join(', ') %></p>
    <p><%= pokemon.description %></p>
  </div>